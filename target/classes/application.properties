#hibernate config
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect

spring.mvc.view.prefix:/WEB-INF/views/
spring.mvc.view.suffix: .jsp

spring.jpa.show-sql = true

spring.queries.users-query=select email, password, active from tsr_user where email=?
spring.queries.roles-query=select u.email, r.role from tsr_user u inner join user_role ur on(u.user_id=ur.user_id) inner join role r on(ur.role_id=r.role_id) where u.email=?

## Hibernate Properties

# create and drop tables and sequences, loads import.sql
spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.hibernate.ddl-auto = update
# Oracle settings
spring.datasource.url=jdbc:oracle:thin:@tsrdbaws.cip0diazjxqt.us-east-2.rds.amazonaws.com:1521:TSRDB
spring.datasource.username=tsrdb
spring.datasource.password=Wizard1919
spring.datasource.driver.class=oracle.jdbc.driver.OracleDriver


spring.messages.basename=validation

# logging
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
logging.level.org.hibernate.SQL=debug

hibernate.id.new_generator_mappings=true

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.profiles.active=dev